# HandMaster 1.0.0 (sample package)

This folder is a sample package layout included for easy local inspection.

Structure:
- `handmaster/` — package source
- `requirements.txt` — Python runtime dependencies
- `run.py` — example launcher

Use this copy for local testing or as an example of how releases are packaged.
